/**
 * Single source of truth for the app version shown in the UI.
 *
 * Keep this in sync with the "version" field in package.json and add a
 * matching entry to CHANGELOG.md on every change.
 *
 * Versioning follows semver (MAJOR.MINOR.PATCH):
 *   - PATCH (0.0.x) — small fixes and tweaks
 *   - MINOR (0.x.0) — a new feature or screen
 *   - MAJOR (x.0.0) — a major overhaul / finished release
 */
export const APP_VERSION = '0.100.8';
